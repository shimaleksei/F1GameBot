# F1 Game Bot

Telegram бот для виртуальных ставок на топ-3 гонщиков Формулы 1 перед каждой гонкой.

## Возможности

- 📊 Ставки на топ-3 гонщиков перед каждой гонкой
- ⏰ Автоматические напоминания за 2 часа до старта
- 🏆 Таблица лидеров с общими очками за сезон
- 📈 Личная статистика и история ставок
- 🔧 Админ-панель для управления гонками и результатами

## Установка

1. Клонируйте репозиторий:
```bash
git clone <repository-url>
cd F1GameBot
```

2. Создайте виртуальное окружение:
```bash
python3 -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

5. Отредактируйте `.env` и укажите:
   - `BOT_TOKEN` - токен бота от @BotFather
   - `ADMIN_IDS` - ID администраторов (через запятую)

6. Запустите бота:
```bash
python bot.py
```

## Получение токена бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям и получите токен
4. Вставьте токен в файл `.env`

## Получение вашего Telegram ID

1. Найдите [@userinfobot](https://t.me/userinfobot) в Telegram
2. Отправьте команду `/start`
3. Бот вернет ваш ID
4. Добавьте ID в `ADMIN_IDS` в файле `.env`

## Структура проекта

```
F1GameBot/
├── bot.py                 # Главный файл бота
├── config.py              # Конфигурация
├── database.py            # Модели базы данных
├── handlers/              # Обработчики команд
│   └── user_handlers.py
├── services/              # Бизнес-логика
│   └── user_service.py
├── models/                # Модели данных
├── utils/                 # Утилиты
└── instructions/          # Документация
    └── instructions.md
```

## Команды бота

### Для всех пользователей:
- `/start` или `/help` - справка
- `/bet` - сделать ставку
- `/my_bets` - мои ставки
- `/leaderboard` - таблица лидеров
- `/me` или `/mystats` - моя статистика

### Для администраторов:
- `/admin_races` - управление гонками
- `/upload_races` - загрузить календарь гонок
- `/results` - ввести результаты гонки

## Технологии

- **aiogram 3.13** - фреймворк для Telegram ботов
- **SQLAlchemy 2.0** - ORM для работы с БД
- **aiosqlite** - асинхронный драйвер для SQLite
- **APScheduler** - планировщик задач для напоминаний
- **python-dotenv** - управление переменными окружения

## Лицензия

MIT

