# F1 Game Bot - Environment Variables Template
# Copy this file to .env and fill in your actual values

# Bot token from @BotFather
BOT_TOKEN=your_bot_token_here

# Admin user IDs (comma-separated, no spaces)
# Get your ID from @userinfobot in Telegram
ADMIN_IDS=your_telegram_id,another_admin_id_if_needed

# Database path (default: f1bot.db)
DATABASE_PATH=f1bot.db

# Timezone for race times (default: UTC)
# Examples: Europe/Belgrade, Europe/Moscow, UTC
# Note: Both TIMEZONE and DEFAULT_TIMEZONE are supported
TIMEZONE=Europe/Belgrade

# Bet closing offset in minutes before race start (default: 5)
# Bets will close this many minutes before race start time
BET_CLOSING_OFFSET=1
